#include "max_pooler.h"
using namespace Convolutional; 
using namespace SubSampler; 
using namespace Pooler;

auto MaxPooler::ProcessMatrix(Matrix::Position position, const Matrix & net) const -> Matrix {
	return Matrix({});
}
